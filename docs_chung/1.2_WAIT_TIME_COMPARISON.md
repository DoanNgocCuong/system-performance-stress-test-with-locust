# ⏱️ So Sánh Wait Time Giữa Các Repo

## 📊 Wait Time Mặc Định

| Repo | Wait Time | Config Location | Giá Trị |
|------|-----------|----------------|---------|
| **4_Robot_Workflow** | `between(1.0, 3.0)` | `config.py` → `locustfile.py` | **1-3 giây** |
| **3_ContextHandling_Robot** | `between(1, 3)` | `locustfile.py` (hardcode) | **1-3 giây** |
| **6_SmallAPI** | `between(0.01, 0.1)` | `config.py` → `locustfile.py` | **0.01-0.1 giây** |

## 🔍 Chi Tiết Từng Repo

### 1. 4_Robot_Workflow

**File:** `4_Robot_Workflow/src/config.py`
```python
WAIT_TIME_MIN = _get_float("ROBOT_WORKFLOW_WAIT_MIN", 1.0)
WAIT_TIME_MAX = _get_float("ROBOT_WORKFLOW_WAIT_MAX", 3.0)
```

**File:** `4_Robot_Workflow/src/locustfile.py`
```python
wait_time = between(Config.WAIT_TIME_MIN, Config.WAIT_TIME_MAX)
# = between(1.0, 3.0) giây
```

**Mặc định:** **1-3 giây**

---

### 2. 3_ContextHandling_Robot

**File:** `3_ContextHandling_Robot/src/locustfile.py`
```python
# Thời gian chờ giữa các task (1-3 giây)
wait_time = between(1, 3)
```

**Mặc định:** **1-3 giây** (hardcode, không có config)

---

### 3. 6_SmallAPI (Hiện tại)

**File:** `6_SmallAPI/src/config.py`
```python
WAIT_TIME_MIN = _get_float("QWEN_API_WAIT_MIN", 0.01)
WAIT_TIME_MAX = _get_float("QWEN_API_WAIT_MAX", 0.1)
```

**File:** `6_SmallAPI/src/locustfile.py`
```python
wait_time = between(Config.WAIT_TIME_MIN, Config.WAIT_TIME_MAX)
# = between(0.01, 0.1) giây
```

**Mặc định:** **0.01-0.1 giây** (rất thấp, cho stress test)

---

## 📈 So Sánh

| Repo | Wait Time | Mục Đích | RPS (100 users) |
|------|-----------|----------|-----------------|
| **4_Robot_Workflow** | 1-3s | Load test thực tế | ~25-50 RPS |
| **3_ContextHandling_Robot** | 1-3s | Load test thực tế | ~25-50 RPS |
| **6_SmallAPI** | 0.01-0.1s | Stress test | ~800-1000 RPS |

## ✅ Kết Luận

- **4_Robot_Workflow**: **1-3 giây** (load test)
- **3_ContextHandling_Robot**: **1-3 giây** (load test)
- **6_SmallAPI**: **0.01-0.1 giây** (stress test - rất thấp)

**Lưu ý:** 6_SmallAPI đang dùng wait_time rất thấp (0.01-0.1s) để stress test, khác với 2 repo kia dùng 1-3s cho load test thực tế.

